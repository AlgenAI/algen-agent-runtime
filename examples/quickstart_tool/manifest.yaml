id: quickstart_tool
display_name: "Tool Quickstart"
tier: quickstart
status: placeholder
description: >
  Introduces one typed read-only tool before showing write-side-effect tools. Not yet implemented.

extras: []
external_services: []
secrets: []

entry_points:
  cli: examples.quickstart_tool.app

commands:
  setup: "pip install -e '.[dev]'"
  run: "python -m examples.quickstart_tool.app"
  test: "python -m pytest examples/quickstart_tool/tests/ -q"
  reset: "echo 'No persistent state to reset.'"

expected_duration_seconds: 10
deterministic_mock_support: false
